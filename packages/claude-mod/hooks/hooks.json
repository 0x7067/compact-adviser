{
  "description": "compact-adviser hooks module: may load through CLAUDE_CODE_ENABLE_FUNCTION_HOOKS or Claude Code's rollout flag, but activates only when CLAUDE_CODE_ENABLE_FUNCTION_HOOKS is exactly 1 and is otherwise a complete no-op",
  "modules": ["./register.ts"]
}
